/*
  Write a function that executes a callback function after a given 
  delay in milliseconds. The default value of delay is one second. 
*/

executeCallback = () => {
  setTimeout(()=>{console.log("Timeout called")},500)
}

executeCallback();
